import React from 'react';
import Content from '../components/commons/content';

/**
 * Post index
 * @param {object} data
 */
const Index = (data) => {
  return (
    <Content>
      Welcome
    </Content>
  );
};

Index.PropsType = {};

export default Index;
